.row
  .small-12-columns
    %h1 Use WordShock!

.row
  .small-12-columns
    = form_for @user_bad_words do |f|
      = f.label :bad_word
      = f.collection_select(:bad_word, :bad_word_id, BadWord.all, :id, :word)

      = f.submit
